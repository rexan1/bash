#! /bin/bash 

OF=/var/my-backup-$(date +%Y%m%d).tgz
           tar -cZf $OF /home/me/

d=$(date +%Y-%m-%d)

find /directory_path -mtime -2 -ls 

find test -mmin -40 | xargs tar --no-recursion -czf $(date +%Y%m%d).tgz      

#find . -mtime -30 -type f -print0  | tar czf changed.tar.gz --files-from=- --null
#echo "$d"
